DROP TABLE PHONE;

DROP TABLE EMP;

CREATE TABLE EMP(EMP_ID NUMBER(5), NAME VARCHAR2(10), SALARY NUMBER(5));

CREATE TABLE PHONE(PHONE_ID NUMBER(5), EMP_ID NUMBER(5), PHONE_NUMBER VARCHAR2(5));

ALTER TABLE EMP ADD (CONSTRAINT PK_EMP_ID PRIMARY KEY(EMP_ID));

ALTER TABLE PHONE ADD (CONSTRAINT PK_PHONE_ID PRIMARY KEY(PHONE_ID));

ALTER TABLE PHONE ADD (CONSTRAINT FK_PHONE_EMP_ID FOREIGN KEY(EMP_ID) REFERENCES EMP(EMP_ID));

